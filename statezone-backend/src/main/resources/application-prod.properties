# Production overrides
app.security.cookie-secure=true
app.security.allowed-origins=https://your-production-domain.com

# In production ensure datasource points to real DB
# spring.datasource.url=jdbc:postgresql://prod-db:5432/statzone
